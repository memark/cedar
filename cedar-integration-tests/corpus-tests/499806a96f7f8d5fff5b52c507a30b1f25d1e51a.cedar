forbid(
  principal is a in a::"V",
  action in [Action::"action"],
  resource in a::""
) when {
  true && [[false == principal, principal, principal, principal, principal, principal], principal, principal, principal, principal, principal]
};
