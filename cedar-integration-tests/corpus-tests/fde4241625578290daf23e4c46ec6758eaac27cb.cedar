forbid(
  principal,
  action in [Action::"action"],
  resource == a::""
) when {
  (true && ((!(principal.greaterThan(principal, principal, principal))).greaterThan(principal, principal, principal))) && principal
};
