permit(
  principal == a::"",
  action in [Action::"action"],
  resource
) when {
  ((true && [true, false, principal == principal, principal, principal, principal, principal]) && principal) && principal
};
