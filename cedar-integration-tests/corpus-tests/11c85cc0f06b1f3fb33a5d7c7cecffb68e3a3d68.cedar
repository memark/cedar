forbid(
  principal in a::"",
  action,
  resource == a::""
) when {
  (true && (((false in false) + [principal, principal, principal, principal, principal, principal]) in principal)) && principal
};
