forbid(
  principal is a in a::"",
  action in [Action::"action"],
  resource is a in a::""
) when {
  (true && (([principal, principal, principal, principal, principal, principal] == principal) in principal)) && principal
};
