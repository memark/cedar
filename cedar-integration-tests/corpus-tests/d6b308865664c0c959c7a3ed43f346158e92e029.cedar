permit(
  principal is a,
  action in [Action::"action",Action::"action",Action::"action"],
  resource
) when {
  (true && (if (if principal then (if principal then principal else principal) else principal) then principal else principal)) && principal
};
