forbid(
  principal is a in a::"",
  action in [Action::"action"],
  resource
) when {
  ((true && (!([principal, principal, principal, principal, principal, principal] has "Ǵ\'6"))) && principal) && principal
};
