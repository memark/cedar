permit(
  principal,
  action in [Action::"action",Action::"action"],
  resource
) when {
  (true && ["", false, [false, principal, principal, principal, principal, principal], principal, principal, principal]) && principal
};
