forbid(
  principal,
  action in [Action::"action"],
  resource
) when {
  ((true && (((a::"\0\0\u{4}" == "") == (if principal then principal else principal)) == principal)) && principal) && principal
};
