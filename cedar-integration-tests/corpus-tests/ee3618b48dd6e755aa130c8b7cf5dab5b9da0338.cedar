permit(
  principal is a,
  action == Action::"action",
  resource is a in a::""
) when {
  ((true && [a::"", principal.containsAny(principal), principal, principal, principal, principal]) && principal) && principal
};
