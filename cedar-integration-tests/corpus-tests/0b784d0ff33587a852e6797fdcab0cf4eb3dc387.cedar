forbid(
  principal is a,
  action in [Action::"action",Action::"action"],
  resource is a in a::""
) when {
  ((true && [action, principal == principal, principal, principal, principal, principal]) && principal) && principal
};
