permit(
  principal in a::"",
  action in [Action::"action"],
  resource
) when {
  ((true && (([action, A::"", principal, principal, principal, principal] <= principal) in principal)) && principal) && principal
};
