permit(
  principal is a in a::"",
  action in [Action::"action"],
  resource is a in a::""
) when {
  ((true && (((false == true) == (principal in principal)) == principal)) && principal) && principal
};
