forbid(
  principal in a::"",
  action in [Action::"action"],
  resource == a::""
) when {
  ((true && (((!principal) && (principal.containsAny(principal))) && principal)) && principal) && principal
};
