forbid(
  principal is a in a::"",
  action,
  resource is a in a::""
) when {
  (true && ([context, principal == principal, principal, principal, principal, principal] == principal)) && principal
};
